## news-to-speech/backend

__Requirements__

* A PostgresSQL and client (run `make create-tables`)
* FFmpeg for PyDub
* Install dependencies with `$ pipenv install`
* Download NLTK package "punkt" after installation
